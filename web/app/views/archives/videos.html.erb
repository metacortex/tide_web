<%= render "shared/splash" %>

<div class="archive">
  <%= render "archives/nav" %>

  <ul class="videos">
    <% @videos.each do |v| %>
      <li>
        <h3><%= v.title %></h3>
        <%= embed_video(v) %>
      </li>
    <% end %>
  </ul>

</div>
