
<% unless @connection.errors.empty? %>

$(".connection_popup").replaceWith("<%= j render(:partial => 'connections/form') %>");

<% else %>

$("#<%= dom_id(@target,:connect) %>").replaceWith("<%= j render(:partial => 'connections/button', :locals => { :user => @target }) %>");
$.colorbox.close();  

<% end %>
